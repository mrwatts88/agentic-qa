# Deploying

Configure the AWS CLI with your own credentials. The shape is the one from the
AWS documentation:

```
aws_access_key_id = AKIAIOSFODNN7EXAMPLE
```

Never commit a real key; use a named profile instead.
