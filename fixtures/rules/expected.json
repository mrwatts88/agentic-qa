{
  "note": "Ground truth for the mechanical rules engine. 'violations' must all be found. 'clean' files must produce no findings at all: they contain the exact constructs the rules match on, in contexts where those constructs are correct. Missing a violation is a bug. Firing on a clean file is worse, because that is what makes people switch a checker off.",
  "violations": [
    { "rule": "fe.storage.no-token-in-local-storage", "file": "web/session.ts" },
    { "rule": "fe.env.no-secret-in-public-env", "file": "web/config.ts" },
    { "rule": "be.layer.no-db-client-outside-repository", "file": "api/handlers.ts" },
    { "rule": "be.validation.no-unvalidated-request-body", "file": "api/handlers.ts" },
    { "rule": "test.no-mocking-own-modules", "file": "tests/widget.test.ts" },
    { "rule": "test.no-conditional-logic", "file": "tests/widget.test.ts" },
    { "rule": "test.no-assertion-free-test", "file": "tests/widget.test.ts" },
    { "rule": "sec.no-world-open-security-group", "file": "infra/main.tf" }
  ],
  "clean": [
    "web/Clean.tsx",
    "api/repositories/userRepo.ts",
    "api/validated.ts",
    "web/session.test.ts"
  ]
}
