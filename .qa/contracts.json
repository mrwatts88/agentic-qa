{
  "version": 1,
  "records": {
    "test/config.test.ts::loadConfig > accepts the .yml spelling of the config file": {
      "id": "test/config.test.ts::loadConfig > accepts the .yml spelling of the config file",
      "file": "test/config.test.ts",
      "description": "accepts the .yml spelling of the config file",
      "descriptionSource": "title",
      "descriptionHash": "13e88d698411731f",
      "bodyHash": "795afd941dd1c31f",
      "verdict": "upheld",
      "reason": "If the loader stops accepting .yml files, the qa.config.yml file would not be loaded, and loadConfig would return the default configuration where failOnUnverifiable is false. This would directly violate the assertion expect(loadConfig(dir).failOnUnverifiable).toBe(true). The test's supporting assertion that DEFAULT_CONFIG.failOnUnverifiable is false ensures this discriminates between file-loaded and default values.",
      "weakestAssertion": "expect(loadConfig(dir).failOnUnverifiable).toBe(true) — this carries all the weight; it fails if the .yml file is not loaded or parsed correctly",
      "suggestedMutation": "Modify loadConfig to only accept .yaml or .json files, ignoring .yml extensions",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:40:41.081Z"
    },
    "test/config.test.ts::loadConfig > falls back to the defaults when the repo has no config file": {
      "id": "test/config.test.ts::loadConfig > falls back to the defaults when the repo has no config file",
      "file": "test/config.test.ts",
      "description": "falls back to the defaults when the repo has no config file",
      "descriptionSource": "title",
      "descriptionHash": "1232f427bb56251c",
      "bodyHash": "0ebe3c66277deaae",
      "verdict": "upheld",
      "reason": "The test setup establishes the precondition (empty directory with no config file) and the assertion directly verifies the claimed behavior via strict structural equality. If loadConfig failed to return DEFAULT_CONFIG when no config file exists, the toEqual assertion would catch it.",
      "weakestAssertion": "The entire expect statement carries the weight. The toEqual(DEFAULT_CONFIG) check is necessary and sufficient; removing or weakening it (e.g., only checking truthiness) would allow broken implementations to pass.",
      "suggestedMutation": "Change loadConfig to return null, undefined, or {} when no config file exists instead of returning DEFAULT_CONFIG",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:31:14.340Z"
    },
    "test/config.test.ts::loadConfig > keeps default judge settings that the config file does not override": {
      "id": "test/config.test.ts::loadConfig > keeps default judge settings that the config file does not override",
      "file": "test/config.test.ts",
      "description": "keeps default judge settings that the config file does not override",
      "descriptionSource": "title",
      "descriptionHash": "88522058819b3a9d",
      "bodyHash": "f334874a3d52ddfd",
      "verdict": "upheld",
      "reason": "The test would fail if the described behavior broke. A realistic break would involve either: (1) the config not loading the file, caught by the model assertion; (2) defaults not being merged/preserved, caught by the three assertions checking concurrency, maxBudgetUsd, and timeoutMs against DEFAULT_CONFIG values. The combination of checking the override works plus checking that other properties retain their defaults makes this test effective.",
      "weakestAssertion": "The assertions comparing against DEFAULT_CONFIG assume the implementation actually uses DEFAULT_CONFIG and doesn't independently hardcode matching values, but this is a reasonable assumption for typical config merging logic.",
      "suggestedMutation": "Remove the default-merging logic from loadConfig so that omitted config file properties are left undefined instead of using defaults. This would cause config.judge.concurrency, config.judge.maxBudgetUsd, and config.judge.timeoutMs to be undefined, failing their respective assertions.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:31:21.921Z"
    },
    "test/config.test.ts::loadConfig > keeps default mutation settings that the config file does not override": {
      "id": "test/config.test.ts::loadConfig > keeps default mutation settings that the config file does not override",
      "file": "test/config.test.ts",
      "description": "keeps default mutation settings that the config file does not override",
      "descriptionSource": "title",
      "descriptionHash": "f7ea1a1e67e4e29a",
      "bodyHash": "67021c7d4a9da694",
      "verdict": "upheld",
      "reason": "The description is specific and falsifiable. The test loads a config file that does not override mutation settings and asserts that mutation.root retains its default value. If the implementation broke (by clearing unoverridden settings, failing to merge defaults, or omitting the mutation object), the assertion `expect(loadConfig(dir).mutation.root).toBe(DEFAULT_CONFIG.mutation.root)` would fail because the property would either be undefined or differ from the default.",
      "weakestAssertion": "expect(loadConfig(dir).mutation.root).toBe(DEFAULT_CONFIG.mutation.root)",
      "suggestedMutation": "Modify loadConfig to clear mutation settings when loading a config file: `const result = {...loadedUserConfig}; delete result.mutation; return result;` - this would cause loadConfig(dir).mutation to be undefined, failing the assertion.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:31:40.039Z"
    },
    "test/config.test.ts::loadConfig > reads settings from qa.config.yaml when one is present": {
      "id": "test/config.test.ts::loadConfig > reads settings from qa.config.yaml when one is present",
      "file": "test/config.test.ts",
      "description": "reads settings from qa.config.yaml when one is present",
      "descriptionSource": "title",
      "descriptionHash": "a01e2fd1f04f26e1",
      "bodyHash": "fe85dc1ac4fa0f8d",
      "verdict": "upheld",
      "reason": "The test creates a qa.config.yaml file with specific property values, loads the config, and asserts those specific values are present in the returned config object. If the file-reading behavior broke (file not read, not parsed, or wrong file read), the config object would not contain these values and both assertions would fail. The assertions use strict equality/deep equality checks on concrete values extracted from the yaml file.",
      "weakestAssertion": "expect(config.testGlobs).toEqual([\"*.spec.ts\"]) — This depends implicitly on the default testGlobs being different from [\"*.spec.ts\"], rather than explicitly verifying that the file was read. However, this is still sufficient to catch if the file-reading behavior broke, assuming realistic defaults.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:31:33.461Z"
    },
    "test/evaluate.test.ts::runEval > counts an expected test that the ledger has no verdict for as a mismatch": {
      "id": "test/evaluate.test.ts::runEval > counts an expected test that the ledger has no verdict for as a mismatch",
      "file": "test/evaluate.test.ts",
      "description": "counts an expected test that the ledger has no verdict for as a mismatch",
      "descriptionSource": "title",
      "descriptionHash": "8b0eb580e0fd2b55",
      "bodyHash": "cc6a88f146fdf5a9",
      "verdict": "upheld",
      "reason": "The assertions directly verify the core claim: that a test expected but absent from the ledger is counted as a mismatch. If the implementation broke and failed to detect this mismatch, it would either return true (failing the first assertion) or fail to print meaningful diagnostic output containing \"MISSING\" (failing the second assertion). Both realistic failure modes are caught.",
      "weakestAssertion": "expect(output).toContain(\"MISSING\") — This checks only for the presence of a substring rather than a complete or structured error message, so an unhelpful or garbled output containing \"MISSING\" incidentally would still pass.",
      "suggestedMutation": "Modify runEval to return true when expected.json contains tests not present in the ledger, effectively ignoring the mismatch. This would cause the first assertion to fail immediately.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:32:32.704Z"
    },
    "test/evaluate.test.ts::runEval > fails rather than reporting a clean score when there is no expectations file": {
      "id": "test/evaluate.test.ts::runEval > fails rather than reporting a clean score when there is no expectations file",
      "file": "test/evaluate.test.ts",
      "description": "fails rather than reporting a clean score when there is no expectations file",
      "descriptionSource": "title",
      "descriptionHash": "83e28643719f9a3f",
      "bodyHash": "654714d4a92e0841",
      "verdict": "upheld",
      "reason": "The test would fail if the described behavior broke in a realistic way. If runEval no longer checked for the missing expectations file and instead proceeded normally, it would return true (success) with empty records. This would violate the assertion `expect(runEval(dir, \"expected.json\")).toBe(false)`. The most likely realistic break scenario involves the function returning a truthy value instead of false, which the test explicitly catches.",
      "weakestAssertion": "The single assertion `expect(runEval(dir, \"expected.json\")).toBe(false)` carries all the weight. The test does not verify that no success message is output to stdout/stderr, so it only checks the return value component of \"fails rather than reporting a clean score\". However, in practice, a function returning false would not print success output.",
      "suggestedMutation": "Remove the check for the expectations file so the function proceeds normally even when it's missing, allowing it to return true for an empty records set rather than detecting and failing on the missing file.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:33:30.990Z"
    },
    "test/evaluate.test.ts::runEval > fails when the judge disagrees with the known-correct answer": {
      "id": "test/evaluate.test.ts::runEval > fails when the judge disagrees with the known-correct answer",
      "file": "test/evaluate.test.ts",
      "description": "fails when the judge disagrees with the known-correct answer",
      "descriptionSource": "title",
      "descriptionHash": "03fb52e2b015e716",
      "bodyHash": "3cc315ab4dfbae1f",
      "verdict": "upheld",
      "reason": "The assertion `expect(runEval(dir, \"expected.json\")).toBe(false)` directly checks the core behavior: that the function returns `false` when judge verdicts disagree with expected verdicts. If the implementation broke and `runEval` returned `true` or threw an error in this scenario, the assertion would fail. The test is properly verifiable against the concrete, specific description.",
      "weakestAssertion": "The test only covers one type of verdict disagreement (violated vs upheld) rather than testing the detection logic across multiple verdict pairs. However, this is a scope limitation, not a logical weakness of the assertion itself.",
      "suggestedMutation": "Change `runEval` to return `true` when verdicts disagree, or remove the disagreement detection logic entirely and have it compare only the test IDs.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:32:18.726Z"
    },
    "test/evaluate.test.ts::runEval > passes when every verdict matches the known-correct answer": {
      "id": "test/evaluate.test.ts::runEval > passes when every verdict matches the known-correct answer",
      "file": "test/evaluate.test.ts",
      "description": "passes when every verdict matches the known-correct answer",
      "descriptionSource": "title",
      "descriptionHash": "cfce147dc7c1c20e",
      "bodyHash": "4f7326f16a52e47e",
      "verdict": "upheld",
      "reason": "The assertions directly test the described behavior. When all verdicts in the ledger match the expected verdicts, the test verifies that (1) runEval returns true and (2) the output contains \"2/2 match\". If the matching logic broke and the function no longer correctly identifies matching verdicts, at least one of these assertions would fail.",
      "weakestAssertion": "The output assertion `expect(output).toContain(\"2/2 match\")` is weakest because it only checks that the output contains this substring somewhere, not that the function reports exactly 2 matches or formats the report correctly. However, combined with the return value assertion, it adequately verifies the described behavior.",
      "suggestedMutation": "Modify runEval to always return false and output \"0/2 match\" regardless of whether verdicts match. This would violate the description by making the function fail even when verdicts match, and both assertions would fail.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:31:50.893Z"
    },
    "test/evaluate.test.ts::runEval > reports a condemned good test as a false positive": {
      "id": "test/evaluate.test.ts::runEval > reports a condemned good test as a false positive",
      "file": "test/evaluate.test.ts",
      "description": "reports a condemned good test as a false positive",
      "descriptionSource": "title",
      "descriptionHash": "8322d1a12009d831",
      "bodyHash": "52cdfdd7e4680202",
      "verdict": "upheld",
      "reason": "The description is specific: a test judged as \"violated\" that should be \"upheld\" must be reported as a false positive. The assertions directly verify this by checking that the output contains \"1 false positive(s)\" and \"0 false negative(s)\". Realistic breaks in the comparison or counting logic would cause these assertions to fail.",
      "weakestAssertion": "expect(output).toContain(\"1 false positive(s)\") carries the critical weight—it verifies that the verdict mismatch is properly counted as a false positive. The accompanying check for \"0 false negative(s)\" provides redundancy to catch miscounts.",
      "suggestedMutation": "Change the evaluation logic to classify verdict mismatches as false negatives instead of false positives (reverse the classification), which would cause the assertions to fail because the output would contain \"0 false positive(s)\" and \"1 false negative(s)\".",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:32:22.897Z"
    },
    "test/evaluate.test.ts::runEval > reports a weak test that was waved through as a false negative": {
      "id": "test/evaluate.test.ts::runEval > reports a weak test that was waved through as a false negative",
      "file": "test/evaluate.test.ts",
      "description": "reports a weak test that was waved through as a false negative",
      "descriptionSource": "title",
      "descriptionHash": "315ffb6f68f1b1ce",
      "bodyHash": "ede6ca1a3afac740",
      "verdict": "upheld",
      "reason": "The assertions directly verify that the output reports exactly \"0 false positive(s)\" and \"1 false negative(s)\". Given the single test record with a mismatch between \"upheld\" (ledger) and \"violated\" (expected), if the false negative detection logic breaks in any realistic way, the output counts would differ and at least one assertion would fail. The test correctly targets the described behavior: detecting and reporting when a test was marked as passing when it should have failed.",
      "weakestAssertion": "Both assertions rely on substring matching rather than regex or exact count verification. However, for this specific single-record scenario, any realistic miscounting would change the substring and cause failure. A contrived scenario where the output somehow always contains these exact substrings regardless of input would pass, but that would require the entire function to be broken in a very specific way that doesn't reflect realistic breakage of the false negative detection logic.",
      "suggestedMutation": "Change the false negative detection logic to always report the opposite (report as false positive instead): `if (ledgerVerdictDiffers) { recordAsFalsePositive(); }` instead of recording as false negative. The assertions would then fail because the output would contain \"1 false positive(s)\" and \"0 false negative(s)\".",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:32:48.940Z"
    },
    "test/extract.test.ts::extractRelativeImports > ignores package imports so only first-party code is offered for mutation": {
      "id": "test/extract.test.ts::extractRelativeImports > ignores package imports so only first-party code is offered for mutation",
      "file": "test/extract.test.ts",
      "description": "ignores package imports so only first-party code is offered for mutation",
      "descriptionSource": "title",
      "descriptionHash": "84821f734634e2fa",
      "bodyHash": "dfb959c3f3707754",
      "verdict": "upheld",
      "reason": "The assertions directly verify that package imports are excluded from the results. Assertions 2 and 3 specifically check for the absence of \"node_modules\" and \"vitest\" in the import paths. If the filtering behavior broke and package imports were no longer ignored, these assertions would fail (given that a .test.ts file almost certainly imports vitest). Assertion 1 additionally catches if non-local first-party code is returned.",
      "weakestAssertion": "The first assertion (`imports.every((p) => p.includes(\"/fixtures/runnable/\"))`) is the weakest link—while it verifies locality, it doesn't directly test the described filtering behavior. However, assertions 2 and 3 provide direct coverage of the core behavior.",
      "suggestedMutation": "Remove the filtering logic that excludes paths containing \"node_modules\" or other package identifiers, allowing package imports to be included in the results. This would cause assertion 2 and/or 3 to fail when the test file's vitest imports appear in the extracted imports.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:35:38.951Z"
    },
    "test/extract.test.ts::extractRelativeImports > resolves a relative import with no extension to the TypeScript file on disk": {
      "id": "test/extract.test.ts::extractRelativeImports > resolves a relative import with no extension to the TypeScript file on disk",
      "file": "test/extract.test.ts",
      "description": "resolves a relative import with no extension to the TypeScript file on disk",
      "descriptionSource": "title",
      "descriptionHash": "c2a7da7173f69f1d",
      "bodyHash": "e3400e06f9cb1316",
      "verdict": "upheld",
      "reason": "The assertions would fail if the described behavior broke in realistic ways. The `toHaveLength(1)` assertion would fail if the function stops extracting the relative import or extracts additional imports. The `endsWith` assertion would fail if the function resolves the import to the wrong file, to a non-TypeScript file, or fails to apply the resolution. Together, these catch the main failure modes: import not extracted, import resolved to wrong location, or import resolved with wrong extension.",
      "weakestAssertion": "The `toHaveLength(1)` check, which assumes `cart.test.ts` contains exactly one relative import and would fail if this assumption is violated, though this is implicit in the test setup and we must trust it.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:34:49.018Z"
    },
    "test/extract.test.ts::extractTests > builds an id from the file, the describe chain and the title": {
      "id": "test/extract.test.ts::extractTests > builds an id from the file, the describe chain and the title",
      "file": "test/extract.test.ts",
      "description": "builds an id from the file, the describe chain and the title",
      "descriptionSource": "title",
      "descriptionHash": "bb81663d4824dabf",
      "bodyHash": "aa31a566ef2cd4e1",
      "verdict": "upheld",
      "reason": "The assertion `expect(nested?.id).toBe(\"coupon.test.ts::checkout > when the payment provider declines > does not create an order\")` directly validates that the id includes all three described components (file, describe chain, title) in the correct format. Any realistic breakage of the described behavior—missing file name, missing describe chain, missing title, or format changes—would cause the string comparison to fail.",
      "weakestAssertion": "The test relies on `nested` being defined (non-null), but this is protected by the optional chaining operator `?.`, which would cause the comparison to fail if the test with title \"does not create an order\" doesn't exist in the fixture file.",
      "suggestedMutation": "Modify `extractTests` to return ids that omit the describe chain, such as `\"coupon.test.ts::does not create an order\"` instead of `\"coupon.test.ts::checkout > when the payment provider declines > does not create an order\"`.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:33:15.382Z"
    },
    "test/extract.test.ts::extractTests > collects the helpers and imports a test body depends on": {
      "id": "test/extract.test.ts::extractTests > collects the helpers and imports a test body depends on",
      "file": "test/extract.test.ts",
      "description": "collects the helpers and imports a test body depends on",
      "descriptionSource": "title",
      "descriptionHash": "0a64c94ee08e61f6",
      "bodyHash": "d26e7fe8c8bb74a1",
      "verdict": "upheld",
      "reason": "If the extraction function broke and stopped collecting imports and helpers, the first assertion checking for \"import\" in the context would fail. For realistic breakage scenarios where the feature stops working, at least the first assertion would catch the regression by detecting the absence of import statements in the context.",
      "weakestAssertion": "The second assertion (`expect(any.context).not.toContain(\"rejects an expired coupon\")`), because it validates what should NOT be present rather than what SHOULD be present. More significantly, there is no assertion verifying that helpers are actually collected, despite the description explicitly mentioning both \"helpers and imports.\"",
      "suggestedMutation": "Change the extraction function to only collect the first line of imports, or skip import collection entirely while still collecting helpers - this would cause the first assertion to fail but would not be caught by verifying helpers are present.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:34:42.981Z"
    },
    "test/extract.test.ts::extractTests > finds every test in a file without picking up the describe blocks": {
      "id": "test/extract.test.ts::extractTests > finds every test in a file without picking up the describe blocks",
      "file": "test/extract.test.ts",
      "description": "finds every test in a file without picking up the describe blocks",
      "descriptionSource": "title",
      "descriptionHash": "0c63021a7f77fd78",
      "bodyHash": "3d147b3a7616f577",
      "verdict": "upheld",
      "reason": "The assertions would fail if the described behavior broke. If the implementation incorrectly picked up describe blocks, the length check (5 expected) or the `not.toContain(\"applyCoupon\")` check would fail. If it missed tests, the length check would fail or \"rejects an expired coupon\" would be missing. The three assertions collectively verify both that tests are found and that describe blocks are excluded.",
      "weakestAssertion": "The `toContain(\"rejects an expired coupon\")` check only verifies that one specific test is found, not that ALL tests are found. However, this is paired with the length assertion (5) which would catch missing tests in the typical case.",
      "suggestedMutation": "Modify the implementation to extract describe blocks in addition to test blocks. For example, change the regex or AST visitor to capture both `describe(...)` and `it(...)` calls as test items.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:33:31.847Z"
    },
    "test/extract.test.ts::extractTests > joins a multi-line docblock into one line and strips the comment markers": {
      "id": "test/extract.test.ts::extractTests > joins a multi-line docblock into one line and strips the comment markers",
      "file": "test/extract.test.ts",
      "description": "joins a multi-line docblock into one line and strips the comment markers",
      "descriptionSource": "title",
      "descriptionHash": "c579c20cde627758",
      "bodyHash": "c71c209a2beb8da0",
      "verdict": "upheld",
      "reason": "The test checks a specific expected string via `.toBe()`. If either behavior broke (failing to join multi-line content or failing to strip markers), the extracted string would not match this exact value. The implementation would have to produce multi-line content or include comment markers, which would violate the string equality assertion.",
      "weakestAssertion": "The single `.toBe()` assertion implicitly verifies both behaviors together rather than testing them separately, but it is sufficiently specific that either behavior breaking would cause the test to fail.",
      "suggestedMutation": "Remove the joining logic: keep docblocks as multi-line rather than joining them into one line. The extracted description would contain newlines and would no longer equal the expected single-line string.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:34:14.734Z"
    },
    "test/extract.test.ts::extractTests > reads an @describes docblock even when a line comment sits between it and the test": {
      "id": "test/extract.test.ts::extractTests > reads an @describes docblock even when a line comment sits between it and the test",
      "file": "test/extract.test.ts",
      "description": "reads an @describes docblock even when a line comment sits between it and the test",
      "descriptionSource": "title",
      "descriptionHash": "8a5e39f9cc7b11f7",
      "bodyHash": "0c7c70394862daec",
      "verdict": "upheld",
      "reason": "If the extraction breaks and stops reading @describes docblocks when a line comment sits between them, the test would fail to find the expected description on the \"refuses to stack\" test. The assertion `expect(documented?.description).toBe(...)` would fail because the description either wouldn't be extracted or wouldn't match. Additionally, `expect(documented?.descriptionSource).toBe(\"docblock\")` would fail if the docblock extraction broke.",
      "weakestAssertion": "The second assertion checking the exact description text. If only this text changed slightly but the function still extracted docblocks, the test would fail. However, both assertions together (descriptionSource and description content) strongly validate that extraction succeeded for this scenario.",
      "suggestedMutation": "Modify the extraction function to skip or ignore line comments when looking for docblocks before tests, causing it to fail finding the docblock when a line comment is present between the docblock and the test definition.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:33:46.694Z"
    },
    "test/extract.test.ts::extractTests > records the full chain of enclosing describe blocks in order": {
      "id": "test/extract.test.ts::extractTests > records the full chain of enclosing describe blocks in order",
      "file": "test/extract.test.ts",
      "description": "records the full chain of enclosing describe blocks in order",
      "descriptionSource": "title",
      "descriptionHash": "4308b57f627a9361",
      "bodyHash": "fba83022e355bd3a",
      "verdict": "upheld",
      "reason": "The test directly verifies the described behavior through a concrete equality check. The assertion `expect(nested?.describePath).toEqual([\"checkout\", \"when the payment provider declines\"])` would fail if the extraction function broke in realistic ways: not recording describe blocks, recording them in wrong order, recording an incomplete chain, or recording wrong names. The `.toEqual()` check catches all these failures.",
      "weakestAssertion": "The assertion relies entirely on the fixture file (`coupon.test.ts`) having the expected nested describe structure, but this is a valid arrangement concern rather than a test weakness—the test setup appropriately establishes this precondition.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:33:14.688Z"
    },
    "test/extract.test.ts::extractTests > reports the line each test starts on": {
      "id": "test/extract.test.ts::extractTests > reports the line each test starts on",
      "file": "test/extract.test.ts",
      "description": "reports the line each test starts on",
      "descriptionSource": "title",
      "descriptionHash": "2ff452f67f4c01f0",
      "bodyHash": "c601e915aa305795",
      "verdict": "upheld",
      "reason": "The assertions directly verify that extracted tests include a `line` property with specific expected values. If the function broke and stopped reporting line numbers, or reported incorrect values, the assertions `expect(first?.line).toBe(11)` and `expect(later?.line).toBe(53)` would fail. Any realistic break in line-number reporting would be caught.",
      "weakestAssertion": "Both assertions are equally strong. They use strict equality to verify hardcoded line numbers against extracted test data.",
      "suggestedMutation": "Remove the line number assignment in the extractTests function, or change all reported line numbers to 0. Either mutation would cause these assertions to fail immediately.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:34:16.986Z"
    },
    "test/extract.test.ts::extractTests > uses the test title as the description when there is no docblock": {
      "id": "test/extract.test.ts::extractTests > uses the test title as the description when there is no docblock",
      "file": "test/extract.test.ts",
      "description": "uses the test title as the description when there is no docblock",
      "descriptionSource": "title",
      "descriptionHash": "bee0bd09fbd53889",
      "bodyHash": "e917c3d09868dee9",
      "verdict": "upheld",
      "reason": "The assertions directly verify the two parts of the described behavior: (1) that description is set to the test title, and (2) that descriptionSource is set to \"title\". If the system stopped using the test title as description when there's no docblock, or stopped marking the source as \"title\", at least one assertion would fail. The test assumes the fixture file contains a test \"rejects an expired coupon\" with no docblock, but that assumption is reasonable for a test targeting this specific behavior.",
      "weakestAssertion": "The test does not explicitly verify that the \"rejects an expired coupon\" test has no docblock—it assumes this from the fixture setup. If the fixture were misconfigured, the test might pass anyway by coincidence. However, this is a property of the fixture arrangement, not a weakness in the assertions themselves.",
      "suggestedMutation": "Remove the line that sets `descriptionSource = \"title\"` when a test has no docblock, or change the condition to only use the title as description when a docblock exists. Either change would cause at least one assertion to fail.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:33:55.502Z"
    },
    "test/ledger.test.ts::hashing > changes when the description changes": {
      "id": "test/ledger.test.ts::hashing > changes when the description changes",
      "file": "test/ledger.test.ts",
      "description": "changes when the description changes",
      "descriptionSource": "title",
      "descriptionHash": "d20a459fe2680cec",
      "bodyHash": "a8d575fb91b0fe6c",
      "verdict": "upheld",
      "reason": "The test directly verifies that descriptionHash produces different outputs for different descriptions. If the behavior broke—meaning the hash function no longer varies with description changes—the assertion `expect(...).not.toBe(...)` would fail when the two hashes become equal.",
      "weakestAssertion": "The single assertion `expect(descriptionHash(test({ description: \"one\" }))).not.toBe(descriptionHash(test({ description: \"two\" })))` carries all the weight. It tests only one pair of descriptions and doesn't verify consistency of hashing (same description always produces same hash), but it directly checks the claimed behavior.",
      "suggestedMutation": "Modify descriptionHash to ignore the description field and hash only other properties of the ExtractedTest object. This would cause the two function calls to return identical values, making the assertion fail.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:29:07.592Z"
    },
    "test/ledger.test.ts::hashing > changes when the meaning of the body changes": {
      "id": "test/ledger.test.ts::hashing > changes when the meaning of the body changes",
      "file": "test/ledger.test.ts",
      "description": "changes when the meaning of the body changes",
      "descriptionSource": "title",
      "descriptionHash": "7d5a3d376cd81248",
      "bodyHash": "2c9e41bfc2a3e7d5",
      "verdict": "upheld",
      "reason": "The assertion directly verifies that bodyHash produces different outputs for different body content. If bodyHash became broken and returned identical hashes for different bodies, the assertion `expect(bodyHash(original)).not.toBe(bodyHash(changed))` would fail.",
      "weakestAssertion": "The test relies entirely on a single assertion that compares two hash values. However, this is appropriate for the described behavior, which has a clear binary outcome: either the hashes differ (correct) or they don't (broken).",
      "suggestedMutation": "Change bodyHash to always return the same constant value (e.g., `return \"constant\"`) regardless of the input body. This would make the two hashes equal, causing the `.not.toBe()` assertion to fail.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:28:26.075Z"
    },
    "test/ledger.test.ts::hashing > ignores whitespace so reformatting a test does not trigger a re-judge": {
      "id": "test/ledger.test.ts::hashing > ignores whitespace so reformatting a test does not trigger a re-judge",
      "file": "test/ledger.test.ts",
      "description": "ignores whitespace so reformatting a test does not trigger a re-judge",
      "descriptionSource": "title",
      "descriptionHash": "e801d23c8f4d4fee",
      "bodyHash": "4c0436e8069133be",
      "verdict": "upheld",
      "reason": "The test directly asserts that `bodyHash` produces identical hashes for bodies differing only in whitespace. If whitespace is no longer ignored, the two hashes would differ and the equality assertion would fail.",
      "weakestAssertion": "The `expect(bodyHash(original)).toBe(bodyHash(reformatted))` assertion carries the full weight—it directly tests that the hashes are equal. If this assertion passed when it should fail, the test would not verify the requirement.",
      "suggestedMutation": "Modify `bodyHash` to hash the body string without normalizing whitespace (e.g., pass the raw body string to a hash function instead of stripping/trimming it first). This would cause the hashes to differ and the assertion to fail.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:28:28.105Z"
    },
    "test/ledger.test.ts::needsJudging > judges a test that has never been seen before": {
      "id": "test/ledger.test.ts::needsJudging > judges a test that has never been seen before",
      "file": "test/ledger.test.ts",
      "description": "judges a test that has never been seen before",
      "descriptionSource": "title",
      "descriptionHash": "64d04d30b32ffdff",
      "bodyHash": "9afed855a83cd674",
      "verdict": "upheld",
      "reason": "The assertion directly checks that needsJudging() returns true when the test has no record in the ledger (represented by an empty records object). The test correctly arranges the scenario described and asserts the expected return value. If the implementation broke and stopped returning true for new/unseen tests, the assertion would fail.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:28:27.784Z"
    },
    "test/ledger.test.ts::needsJudging > re-judges everything when the judge version moved on": {
      "id": "test/ledger.test.ts::needsJudging > re-judges everything when the judge version moved on",
      "file": "test/ledger.test.ts",
      "description": "re-judges everything when the judge version moved on",
      "descriptionSource": "title",
      "descriptionHash": "98c1e96be98797d3",
      "bodyHash": "df90e46817c75208",
      "verdict": "upheld",
      "reason": "The test directly verifies that needsJudging returns true when a ledger entry has an older judge version. If the judge version check was removed or broken, the function would return false and the assertion would fail.",
      "weakestAssertion": "expect(needsJudging(t, stale, \"haiku\")).toBe(true) — the single assertion in the test, which would fail if the function did not properly detect stale judge versions.",
      "suggestedMutation": "Remove the judge version check from the needsJudging implementation, or change it to ignore judgeVersion differences. This would cause the function to return false for this case, failing the assertion.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:28:59.244Z"
    },
    "test/ledger.test.ts::needsJudging > re-judges when a different model is asked for": {
      "id": "test/ledger.test.ts::needsJudging > re-judges when a different model is asked for",
      "file": "test/ledger.test.ts",
      "description": "re-judges when a different model is asked for",
      "descriptionSource": "title",
      "descriptionHash": "4c8fa6813797ea17",
      "bodyHash": "7f761cc78e9a52fc",
      "verdict": "upheld",
      "reason": "The test would fail if the described behavior broke. It specifically tests that needsJudging returns true when a different model (\"sonnet\") is requested than what's stored in the ledger (\"haiku\"). Realistic bugs that would violate the model-checking behavior—such as removing the model comparison, inverting it, or ignoring the model parameter—would all cause needsJudging to return false, making the assertion fail.",
      "weakestAssertion": "The test relies on needsJudging correctly implementing model comparison, but provides no explicit visibility into which condition caused the return value. If needsJudging had multiple conditions that could return true and one of them was accidentally always-true, the test might pass for the wrong reason—though such a bug would require an unrealistic implementation.",
      "suggestedMutation": "Remove the model comparison logic from needsJudging—for example, change it to return false whenever a record exists, without checking if the requested model matches. This would make needsJudging return false for this test while the described behavior (re-judging when models differ) would be broken.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:30:20.864Z"
    },
    "test/ledger.test.ts::needsJudging > re-judges when the description changed but the body did not": {
      "id": "test/ledger.test.ts::needsJudging > re-judges when the description changed but the body did not",
      "file": "test/ledger.test.ts",
      "description": "re-judges when the description changed but the body did not",
      "descriptionSource": "title",
      "descriptionHash": "76c61ac049a10a7d",
      "bodyHash": "c90c7fc9ffca1087",
      "verdict": "upheld",
      "reason": "The assertion directly tests the described behavior: when a test's description changes but its body remains the same, `needsJudging` should return `true`. If the implementation broke and failed to detect the description change (the most realistic failure mode), it would incorrectly return `false`, causing the assertion `expect(needsJudging(reworded, ledger, \"haiku\")).toBe(true)` to fail.",
      "weakestAssertion": "The single assertion: `expect(needsJudging(reworded, ledger, \"haiku\")).toBe(true)`. This is the only verification point, but it squarely targets the described behavior.",
      "suggestedMutation": "Modify `needsJudging` to ignore description hash changes and only return `true` if the body hash differs from what's recorded in the ledger. This would cause the test to fail since the body hashes match but the description has changed.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:29:21.584Z"
    },
    "test/ledger.test.ts::needsJudging > re-judges when the test body changed": {
      "id": "test/ledger.test.ts::needsJudging > re-judges when the test body changed",
      "file": "test/ledger.test.ts",
      "description": "re-judges when the test body changed",
      "descriptionSource": "title",
      "descriptionHash": "d891644a495b7044",
      "bodyHash": "0d8f0d564c9893e6",
      "verdict": "upheld",
      "reason": "The test changes only the body of a test and expects `needsJudging` to return `true`. If the implementation fails to detect body changes—a realistic failure mode—the function would return `false` and the assertion would fail. This directly tests whether the described behavior of re-judging when the test body changed is working.",
      "weakestAssertion": "The assertion `expect(needsJudging(edited, ledger, \"haiku\")).toBe(true)` only checks that the return value is `true`, not that the body change is the specific reason for that value. However, given the test setup where only the body differs, this is adequate to verify the described behavior.",
      "suggestedMutation": "Remove the body hash comparison from the `needsJudging` implementation, or invert its logic so that matching body hashes incorrectly return true instead of false. This would cause the function to return false when the body changes, violating the assertion.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:29:49.331Z"
    },
    "test/ledger.test.ts::needsJudging > skips a test whose description, body, model and judge are all unchanged": {
      "id": "test/ledger.test.ts::needsJudging > skips a test whose description, body, model and judge are all unchanged",
      "file": "test/ledger.test.ts",
      "description": "skips a test whose description, body, model and judge are all unchanged",
      "descriptionSource": "title",
      "descriptionHash": "df65fbae0266e533",
      "bodyHash": "039e8f5dff01590f",
      "verdict": "upheld",
      "reason": "The assertion directly tests the claimed behavior: when description, body, model, and judge version all match between a test and its ledger entry, needsJudging returns false. The test correctly establishes the precondition (matching values) and checks the expected outcome (false). If the implementation broke and returned true, the assertion would fail.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:28:41.299Z"
    },
    "test/ledger.test.ts::prune > drops records for tests that no longer exist and reports how many went": {
      "id": "test/ledger.test.ts::prune > drops records for tests that no longer exist and reports how many went",
      "file": "test/ledger.test.ts",
      "description": "drops records for tests that no longer exist and reports how many went",
      "descriptionSource": "title",
      "descriptionHash": "eb525a1c5bb05b21",
      "bodyHash": "a7af4dc591d33c3e",
      "verdict": "upheld",
      "reason": "The two assertions together verify both key aspects of the described behavior: that records for non-existent tests are removed (checked by the second assertion on remaining records) and that the count is reported correctly (checked by the first assertion on the return value). Any realistic break in either removing the correct records or returning the correct count would cause at least one assertion to fail.",
      "weakestAssertion": "expect(removed).toBe(1) - This checks only the return value and would pass if the ledger modification succeeded but the wrong count was returned. However, the second assertion complements this by verifying the actual state change.",
      "suggestedMutation": "Change prune to remove the record but return 0 instead of the actual removal count - this would make the first assertion fail while the second would still pass, proving the first assertion is necessary.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:29:40.685Z"
    },
    "test/ledger.test.ts::prune > keeps every record when all of them are still live": {
      "id": "test/ledger.test.ts::prune > keeps every record when all of them are still live",
      "file": "test/ledger.test.ts",
      "description": "keeps every record when all of them are still live",
      "descriptionSource": "title",
      "descriptionHash": "7d2ca7450f52fe47",
      "bodyHash": "3e255312965b5fa7",
      "verdict": "upheld",
      "reason": "The assertions directly verify the described behavior. When all records are live (their IDs are in the liveSet), the test checks that (1) prune reports 0 removals and (2) the record actually remains in the ledger. If prune broke and started removing records that should be kept, either assertion would fail—either the return value would be non-zero or the record count would drop below 1.",
      "weakestAssertion": "expect(prune(ledger, new Set([t.id]))).toBe(0) — This checks an implementation detail (return value) rather than the core behavior. The second assertion more directly verifies that records are actually kept.",
      "suggestedMutation": "Modify prune to remove records based on a secondary criterion even when they're live: e.g., remove records whose IDs start with 'a.' regardless of whether they're in liveSet. This would make the test fail because prune would return 1 and ledger.records would have length 0.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:30:26.690Z"
    },
    "test/rules.test.ts::routing > applies a rule to a file its trigger glob matches": {
      "id": "test/rules.test.ts::routing > applies a rule to a file its trigger glob matches",
      "file": "test/rules.test.ts",
      "description": "applies a rule to a file its trigger glob matches",
      "descriptionSource": "title",
      "descriptionHash": "a5b6cf37c80e7472",
      "bodyHash": "0ca8ab32350cc7cb",
      "verdict": "upheld",
      "reason": "The test verifies that `ruleAppliesTo` returns `true` when a file path matches the rule's trigger glob pattern. For realistic ways the behavior could break (removal of glob matching logic, inverted logic, glob pattern parsing errors, or comparison bugs), the function would return `false` instead, causing the assertion to fail. The test would catch these broken implementations.",
      "weakestAssertion": "The single assertion `expect(ruleAppliesTo(rule(), \"api/handlers.ts\")).toBe(true)` is the only check; the test only verifies the positive case (file matching glob) and does not verify the negative case (file not matching glob), though this limitation does not make it violated.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:36:33.535Z"
    },
    "test/rules.test.ts::routing > collects the trigger globs so only relevant paths get walked": {
      "id": "test/rules.test.ts::routing > collects the trigger globs so only relevant paths get walked",
      "file": "test/rules.test.ts",
      "description": "collects the trigger globs so only relevant paths get walked",
      "descriptionSource": "title",
      "descriptionHash": "146c42033c76ba0b",
      "bodyHash": "addaf26b50dd53fa",
      "verdict": "upheld",
      "reason": "The assertions directly validate that triggerGlobs() collects trigger path globs from multiple rules. Both `.toContain()` assertions check for specific glob strings from the rules' trigger paths. If the function failed to collect globs, returned empty results, or modified the globs, at least one assertion would fail.",
      "weakestAssertion": "Both assertions have equal weight: `expect(globs).toContain(\"**/*.ts\")` and `expect(globs).toContain(\"**/*.tf\")` each verify collection from one of the two rules provided, ensuring the function doesn't selectively ignore rules.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:36:43.868Z"
    },
    "test/rules.test.ts::routing > does not apply a rule triggered on **/*.ts to a .tf file": {
      "id": "test/rules.test.ts::routing > does not apply a rule triggered on **/*.ts to a .tf file",
      "file": "test/rules.test.ts",
      "description": "does not apply a rule triggered on **/*.ts to a .tf file",
      "descriptionSource": "title",
      "descriptionHash": "6333fcba930018d6",
      "bodyHash": "97150547e9b7d66e",
      "verdict": "upheld",
      "reason": "The assertion `expect(ruleAppliesTo(rule(), \"infra/main.tf\")).toBe(false)` directly tests the described behavior: that a rule triggered on `**/*.ts` does not apply to a `.tf` file. If glob pattern matching or the routing logic broke and incorrectly matched Terraform files to TypeScript patterns, the function would return `true`, causing the test to fail. The assertion would reliably catch realistic implementations of this breakage.",
      "weakestAssertion": "The test only verifies one specific `.tf` file (`infra/main.tf`), so it would not catch a bug that somehow selectively matches only certain `.tf` files. However, this narrow scope is appropriate for a single test case and is not a violation of the description, which only claims the rule should not apply to \"a .tf file\".",
      "suggestedMutation": "Remove the file extension check in the glob matching logic, or add a condition that applies all rules regardless of their trigger patterns—either change would cause `ruleAppliesTo(rule(), \"infra/main.tf\")` to return `true` and fail the test.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:36:03.893Z"
    },
    "test/rules.test.ts::routing > lets an exclude glob win over a matching trigger glob": {
      "id": "test/rules.test.ts::routing > lets an exclude glob win over a matching trigger glob",
      "file": "test/rules.test.ts",
      "description": "lets an exclude glob win over a matching trigger glob",
      "descriptionSource": "title",
      "descriptionHash": "d8bdc11dd57cff4a",
      "bodyHash": "0dfa8c2f82b2bd7d",
      "verdict": "upheld",
      "reason": "The test directly asserts that paths matching both a trigger glob and an exclude glob are excluded from the rule (the exclude glob wins). The second assertion `expect(ruleAppliesTo(layered, \"api/repositories/userRepo.ts\")).toBe(false)` would fail if the exclude logic broke or was removed, or if the implementation mistakenly applied rules to excluded paths.",
      "weakestAssertion": "The first assertion is weaker since it only tests the positive case (a matching trigger with no exclude). The second assertion carries the weight by testing the core claim: that excludes override triggers.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:36:01.048Z"
    },
    "test/rules.test.ts::routing > treats windows-style separators the same as posix ones": {
      "id": "test/rules.test.ts::routing > treats windows-style separators the same as posix ones",
      "file": "test/rules.test.ts",
      "description": "treats windows-style separators the same as posix ones",
      "descriptionSource": "title",
      "descriptionHash": "8a33080e4f7ff0c2",
      "bodyHash": "2577a4aa4bf4d3a7",
      "verdict": "upheld",
      "reason": "The assertion directly verifies the described behavior by checking that normalise() converts Windows-style backslash separators to POSIX forward slashes. If this normalization broke, the toBe() assertion would fail because the returned value would no longer match the expected POSIX path format.",
      "weakestAssertion": "The single assertion `expect(normalise(\"api\\\\repositories\\\\userRepo.ts\")).toBe(\"api/repositories/userRepo.ts\")` fully validates the claimed behavior, though it only tests one input path. However, this is sufficient to verify that Windows separators are being normalized as claimed.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:36:13.111Z"
    },
    "test/rules.test.ts::the bundled rule corpus > filters to the requested packs and leaves the rest out": {
      "id": "test/rules.test.ts::the bundled rule corpus > filters to the requested packs and leaves the rest out",
      "file": "test/rules.test.ts",
      "description": "filters to the requested packs and leaves the rest out",
      "descriptionSource": "title",
      "descriptionHash": "5808dc055813894a",
      "bodyHash": "907453d2ace69c61",
      "verdict": "upheld",
      "reason": "The test verifies filtering behavior through three mutually-reinforcing assertions. The critical assertion `only.every((r) => r.pack === \"testing\")` directly checks that the filtered result contains only the requested pack. The `only.length < all.length` assertion catches no-op filters. Realistic ways the filtering could break—ignoring the parameter, returning wrong packs, inverting the filter, or returning empty results—would all cause at least one assertion to fail.",
      "weakestAssertion": "The first assertion `only.length > 0` is weakest; it only verifies that at least one \"testing\" pack rule exists, which wouldn't catch most filtering bugs. It primarily guards against the `loadRules` function crashing.",
      "suggestedMutation": "Modify the pack filter logic to ignore the requested packs parameter entirely (e.g., `loadRules([], [\"testing\"])` becomes equivalent to `loadRules()`). This would cause either `only.length < all.length` to fail (if mixed packs exist) or `only.every((r) => r.pack === \"testing\")` to fail (if non-\"testing\" packs are returned).",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:42:01.230Z"
    },
    "test/rules.test.ts::the bundled rule corpus > gives every rule an enforcement its tier can actually run": {
      "id": "test/rules.test.ts::the bundled rule corpus > gives every rule an enforcement its tier can actually run",
      "file": "test/rules.test.ts",
      "description": "gives every rule an enforcement its tier can actually run",
      "descriptionSource": "title",
      "descriptionHash": "365c94986385ee5f",
      "bodyHash": "58b7d0529020e6d9",
      "verdict": "upheld",
      "reason": "The assertions directly verify that every rule's enforcement type matches what its tier can execute. The mechanical.every() and llm.every() checks would fail if any rule is assigned an enforcement kind incompatible with its tier, and the length sum check catches rules with unknown tiers. Any realistic break—such as a mechanical rule getting llm enforcement, or a new unsupported tier being added—would cause assertion failure.",
      "weakestAssertion": "The `expect(mechanical.length).toBeGreaterThan(0)` and `expect(llm.length).toBeGreaterThan(0)` checks are sanity checks on the corpus being non-empty but don't directly verify the tier-enforcement matching. The core weight is carried by the `.every()` assertions and the length sum check.",
      "suggestedMutation": "Change a rule in the rules corpus from tier \"mechanical\" with enforcement.kind \"pattern\" to tier \"mechanical\" with enforcement.kind \"llm\". This would violate the described behavior (mechanical tier gets an enforcement it cannot run) but is easy for the test assertions to catch in the mechanical.every() check.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:35:25.151Z"
    },
    "test/rules.test.ts::the bundled rule corpus > loads every bundled pack": {
      "id": "test/rules.test.ts::the bundled rule corpus > loads every bundled pack",
      "file": "test/rules.test.ts",
      "description": "loads every bundled pack",
      "descriptionSource": "title",
      "descriptionHash": "cadec9813d630ef9",
      "bodyHash": "3425073c765d7411",
      "verdict": "upheld",
      "reason": "The test asserts that loadRules() returns rules with exactly these four pack names: backend, frontend, security, and testing. Any realistic breakage of \"loads every bundled pack\" would result in either missing one of these packs or adding an unexpected one, both of which would cause the equality assertion to fail. If the backend pack fails to load, or if a new pack is added, or if the loading mechanism breaks entirely, the test would catch it.",
      "weakestAssertion": "The test only validates that the exact set of pack names matches the expected array. It doesn't verify that these packs are loaded from bundled files specifically, only that they appear in the result. However, this is acceptable for a unit test at this level.",
      "suggestedMutation": "Remove or rename one of the bundled pack files (e.g., delete the backend pack directory), which would cause loadRules() to omit that pack and make the test fail.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:34:58.949Z"
    },
    "test/rules.test.ts::the bundled rule corpus > rejects a malformed rule loudly instead of silently dropping it": {
      "id": "test/rules.test.ts::the bundled rule corpus > rejects a malformed rule loudly instead of silently dropping it",
      "file": "test/rules.test.ts",
      "description": "rejects a malformed rule loudly instead of silently dropping it",
      "descriptionSource": "title",
      "descriptionHash": "6c3bce02e5c10a04",
      "bodyHash": "b14e2f4d63be79bc",
      "verdict": "upheld",
      "reason": "The assertion `.toThrow(/tier must be/)` directly verifies the described behavior. It would fail both if the rule were silently dropped (no error thrown) or if an error were thrown with a different message. The test correctly checks that malformed rules produce an observable error rather than being quietly ignored.",
      "weakestAssertion": "The entire `expect(() => loadRules([...])).toThrow(/tier must be/)` check. Both components are necessary: `.toThrow()` ensures the rule isn't silently dropped, and the `/tier must be/` regex ensures the error is about validation, not some other failure.",
      "suggestedMutation": "Change `loadRules` to silently skip rules with invalid tier values instead of throwing an error. The test would then fail because `loadRules` would not throw.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:35:55.632Z"
    },
    "test/rules.test.ts::the mechanical runner > reports a file and line for a matching pattern": {
      "id": "test/rules.test.ts::the mechanical runner > reports a file and line for a matching pattern",
      "file": "test/rules.test.ts",
      "description": "reports a file and line for a matching pattern",
      "descriptionSource": "title",
      "descriptionHash": "6bca8a2437a844e9",
      "bodyHash": "818e5e00858a783b",
      "verdict": "upheld",
      "reason": "The assertions directly verify the two requirements of the description: `expect(finding.file).toBe(\"a.ts\")` confirms the file is reported, and `expect(finding.line).toBe(2)` confirms the line number is reported correctly for a matching pattern. Both assertions would fail if the implementation reported incorrect file paths or line numbers.",
      "weakestAssertion": "The `expect(finding.excerpt).toContain(\"forbidden\")` assertion is somewhat weaker than the core assertions, as it only checks that the excerpt contains the pattern anywhere, not that the match was specifically found on the reported line. However, this is secondary to the file and line assertions.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:36:28.103Z"
    },
    "test/rules.test.ts::the mechanical runner > reports a rule once per file however many times it matches": {
      "id": "test/rules.test.ts::the mechanical runner > reports a rule once per file however many times it matches",
      "file": "test/rules.test.ts",
      "description": "reports a rule once per file however many times it matches",
      "descriptionSource": "title",
      "descriptionHash": "a9904684dfed0f10",
      "bodyHash": "a00722a58ab03d7f",
      "verdict": "upheld",
      "reason": "The test directly asserts that runMechanical returns exactly one report when a pattern matches three times in a single file. If the implementation broke and began reporting once per match instead of once per file, the length would be 3 instead of 1, causing toHaveLength(1) to fail. The assertion is precisely calibrated to catch breaks in the described \"once per file\" behavior.",
      "weakestAssertion": "toHaveLength(1) - This is the only assertion in the test, and it carries full responsibility for verifying the claim. It would need to be paired with additional assertions checking what is actually reported to verify which specific rule is being reported, but that is beyond the scope of this description which focuses on reporting count per file.",
      "suggestedMutation": "Change the implementation to report each individual match as a separate result instead of deduplicating by file. This would cause runMechanical to return 3 results instead of 1, making toHaveLength(1) fail.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:37:05.457Z"
    },
    "test/rules.test.ts::the mechanical runner > reports the line the match is actually on, not a blank line above it": {
      "id": "test/rules.test.ts::the mechanical runner > reports the line the match is actually on, not a blank line above it",
      "file": "test/rules.test.ts",
      "description": "reports the line the match is actually on, not a blank line above it",
      "descriptionSource": "title",
      "descriptionHash": "498c970597afe534",
      "bodyHash": "07c282afb2fd3252",
      "verdict": "upheld",
      "reason": "The description is specific and falsifiable. The test creates an exact scenario with a blank line above the target match and directly asserts that the finding's line number is 4 (the actual match) rather than 3 (the blank line above). The excerpt assertion provides confirmation. If the implementation broke and reported the blank line's position or wrong content, these assertions would fail.",
      "weakestAssertion": "expect(finding.line).toBe(4) - this is the primary assertion validating the core claim that the line reported is the actual match location, not a blank line above it",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:36:32.102Z"
    },
    "test/rules.test.ts::the mechanical runner > skips a file no active rule applies to": {
      "id": "test/rules.test.ts::the mechanical runner > skips a file no active rule applies to",
      "file": "test/rules.test.ts",
      "description": "skips a file no active rule applies to",
      "descriptionSource": "title",
      "descriptionHash": "e0e1d07e53fe5f7b",
      "bodyHash": "208268c08f992640",
      "verdict": "upheld",
      "reason": "The test directly verifies that when a file (main.tf) doesn't match any rule's trigger patterns (rule triggers on **/*.ts only), the mechanical runner returns no violations. If the runner failed to skip such files and applied all rules regardless of triggers, the pattern \"forbidden\" would match and violations would be reported. The assertion `expect(runMechanical(dir, [\"main.tf\"], [rule()])).toEqual([])` would fail in this realistic breaking scenario.",
      "weakestAssertion": "The assertion `expect(runMechanical(dir, [\"main.tf\"], [rule()])).toEqual([])` is the sole assertion and it is appropriately strong—it requires exactly an empty array, not just something falsy or array-like.",
      "suggestedMutation": "Modify the runner to apply all rules to all files regardless of whether the file matches the rule's trigger patterns. This would cause the \"forbidden\" pattern to match the file content and report a violation, making the result non-empty and failing the assertion.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:37:15.804Z"
    },
    "test/rules.test.ts::the mechanical runner > stays silent when the file also contains the exempting pattern": {
      "id": "test/rules.test.ts::the mechanical runner > stays silent when the file also contains the exempting pattern",
      "file": "test/rules.test.ts",
      "description": "stays silent when the file also contains the exempting pattern",
      "descriptionSource": "title",
      "descriptionHash": "cfcc66165815a93c",
      "bodyHash": "a8dfe81767d71b5d",
      "verdict": "upheld",
      "reason": "The description is specific and falsifiable. The test correctly arranges a file containing both the matching pattern (\"forbidden\") and the exempting pattern (\"validate(\"), then asserts that no violations are reported. If the unlessFilePattern exemption logic broke, the rule would report a violation and the toEqual([]) assertion would fail. The single assertion is appropriately strong for verifying this behavior.",
      "weakestAssertion": "The test relies entirely on a single assertion: `expect(runMechanical(dir, [\"a.ts\"], [conditional])).toEqual([])`. However, this assertion is actually quite strong—it directly verifies the described behavior (no violations reported when exempting pattern is present). There is no weaker element that could let a broken implementation pass.",
      "suggestedMutation": "Remove or comment out the unlessFilePattern logic from the mechanical rule enforcement implementation. This would cause the rule to match the \"forbidden\" pattern and report a violation, making the returned array non-empty and causing the toEqual([]) assertion to fail.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:36:47.528Z"
    },
    "test/rules.test.ts::the mechanical runner > still fires when the exempting pattern is absent": {
      "id": "test/rules.test.ts::the mechanical runner > still fires when the exempting pattern is absent",
      "file": "test/rules.test.ts",
      "description": "still fires when the exempting pattern is absent",
      "descriptionSource": "title",
      "descriptionHash": "dbe105f1c1e527ef",
      "bodyHash": "55ab790c42a2d07d",
      "verdict": "upheld",
      "reason": "The assertion directly checks that the rule produces exactly one result (`.toHaveLength(1)`). If the described behavior broke—i.e., if the rule failed to fire when the exempting pattern was absent—the result would be empty (length 0) and the assertion would fail.",
      "weakestAssertion": "The `.toHaveLength(1)` assertion: if `unlessFilePattern` exemption logic inverted or failed, causing the rule not to fire despite the exempting pattern being absent, the array length would be 0 instead of 1, causing this assertion to fail.",
      "suggestedMutation": "In the enforcement logic, add a condition that exempts the rule when `unlessFilePattern` is missing from the file, rather than when it is present. This would make the rule not fire in this test case (length 0), breaking the assertion while breaking the described behavior.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:36:55.535Z"
    },
    "test/rules.test.ts::the qa-ignore escape hatch > does not suppress a different rule that happens to match the same line": {
      "id": "test/rules.test.ts::the qa-ignore escape hatch > does not suppress a different rule that happens to match the same line",
      "file": "test/rules.test.ts",
      "description": "does not suppress a different rule that happens to match the same line",
      "descriptionSource": "title",
      "descriptionHash": "e8d651466d77a77a",
      "bodyHash": "52a8c5816865e11b",
      "verdict": "upheld",
      "reason": "The assertion `toHaveLength(1)` would fail if the described behavior broke. A realistic break—where qa-ignore incorrectly suppresses rules it shouldn't target—would cause the violation count to drop from 1 to 0, failing the assertion. The test directly verifies that a violation is still reported when qa-ignore targets a different rule.",
      "weakestAssertion": "toHaveLength(1) - this is the only assertion, and it checks the count of violations but not which rule(s) generated them. This means the test doesn't distinguish between \"qa-ignore correctly didn't suppress test.rule\" and \"qa-ignore was never implemented\". However, this distinction doesn't matter for catching realistic breaks of the described behavior.",
      "suggestedMutation": "Modify the qa-ignore suppression logic to apply to all rules on a matched line, not just the specified rule: `if (hasQaIgnore(line)) { suppress() }` instead of `if (hasQaIgnore(line, targetedRuleId)) { suppress() }`",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:37:42.574Z"
    },
    "test/rules.test.ts::the qa-ignore escape hatch > suppresses the named rule on the same line": {
      "id": "test/rules.test.ts::the qa-ignore escape hatch > suppresses the named rule on the same line",
      "file": "test/rules.test.ts",
      "description": "suppresses the named rule on the same line",
      "descriptionSource": "title",
      "descriptionHash": "b621648d315c2775",
      "bodyHash": "e489938b97e185e7",
      "verdict": "upheld",
      "reason": "The test verifies that when a qa-ignore comment naming a specific rule appears on the same line as code matching that rule's pattern, no violations are reported. If the suppression feature broke (qa-ignore parsing, rule ID matching, or suppression logic), and pattern matching remained functional, the rule violation would be reported and the assertion `toEqual([])` would fail. The test directly checks the described behavior.",
      "weakestAssertion": "The single assertion `expect(runMechanical(dir, [\"a.ts\"], [rule()])).toEqual([])` carries all the weight. It verifies the end-to-end result. The test doesn't explicitly verify that the pattern was matched as a precondition, so it depends on pattern matching working correctly—but that's a separate concern from the suppression behavior being tested.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:38:12.040Z"
    },
    "test/rules.test.ts::the qa-ignore escape hatch > suppresses the named rule when the comment is on the line above": {
      "id": "test/rules.test.ts::the qa-ignore escape hatch > suppresses the named rule when the comment is on the line above",
      "file": "test/rules.test.ts",
      "description": "suppresses the named rule when the comment is on the line above",
      "descriptionSource": "title",
      "descriptionHash": "630951beab3e6557",
      "bodyHash": "51e758aac2377f27",
      "verdict": "upheld",
      "reason": "The test is verifiable and the assertion would fail if the suppress behavior broke. If the qa-ignore comment logic is removed or broken, the rule would match the \"forbidden\" pattern on line 2, return a violation in the array, and fail the `toEqual([])` assertion.",
      "weakestAssertion": "The single assertion `expect(runMechanical(dir, [\"a.ts\"], [rule()])).toEqual([])` carries all the weight. It depends on the rule pattern actually matching \"forbidden\" (a reasonable assumption given the rule config), but the test lacks a companion assertion or separate test showing the rule would match without the ignore comment.",
      "suggestedMutation": "Remove or disable the qa-ignore comment logic so that violation suppression no longer works. The test would fail when `runMechanical` returns a violation array instead of an empty array.",
      "model": "haiku",
      "judgeVersion": 4,
      "checkedAt": "2026-09-16T15:37:26.692Z"
    }
  }
}
